// BollyZoneProvider module build config
cloudstream {
    // The human-readable name shown in CloudStream's extension list
    extName = "BollyZone"

    // Your name / handle
    extClass = ".BollyZoneProvider"

    // Extension description shown in the app
    description = "Watch Indian TV Shows and Hindi Dramas from BollyZone"

    // 0 = working, 1 = slow, 2 = broken
    status = 0

    // Icon URL (can update this later with a hosted image)
    iconUrl = "https://www.bollyzone.to/wp-content/uploads/2021/10/bollyzone-logo.png"

    // Version — bump this when you release updates
    version = 1

    // Language of the content
    language = "hi" // Hindi
}
